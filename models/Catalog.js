import { Media } from './Media';

export class Catalog {
    mediaList = new Array();

    static addItem(type, fields) {

    }

    static editem(type, fields) {

    }

    static viewItems() {
        
    }
}