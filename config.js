var config = {
    db: {
        host: '127.0.0.1',
        user: 'dbuser',
        password: 'very-secret',
        db: 'anansi_db'
    }
};

// TODO: import hidden.js and extract credentials
module.exports = config;
